[pytest]
addopts = --cov=app --cov-report=term-missing -m "not live"
testpaths = tests
markers =
    live: requires a real ANTHROPIC_API_KEY and makes a real network call (skipped by default; run with `pytest -m live -o addopts=""`)
